tags:
  - "Workflow"
parameters:
  - in: "body"
    name: "body"
    description: "Workflow object that needs to send"
    required: true
responses:
  200:
    description: "Workflow object"
  400:
    description: "Body message is not a valid json"
  403:
    description: "Body have unsuported status type"